<section class="user-section">
  <h2>
    <%= "Name: #{@user.name}" %>
    <%= raw button(@user, @friend) %>
  </h2>
    <article class="timeline">
      <h3>Recent posts:</h3>
      <ul class="posts">
        <%= post(@user, @friend) %>
      </ul>
    </article>
</section>
